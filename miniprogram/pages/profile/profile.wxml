<!--pages/profile/profile.wxml-->
<view class="page">
    <scroll-view class="scroll-area" scroll-y enhanced show-scrollbar="{{false}}">
        <!-- 用户信息卡片 -->
        <view class="profile-card">
            <view class="profile-info">
                <view class="profile-avatar">{{avatarLetter}}</view>
                <view class="profile-text">
                    <text class="profile-name">{{displayName}}</text>
                    <text wx:if="{{isLoggedIn}}" class="profile-desc">点击右侧按钮可修改</text>
                    <text wx:else class="profile-desc">登录后解锁更多功能</text>
                </view>
            </view>
            <view wx:if="{{isLoggedIn}}" class="profile-action" bindtap="openEdit">修改</view>
        </view>

        <!-- 未登录时显示登录按钮 -->
        <view
            wx:if="{{!isLoggedIn}}"
            class="login-button {{isLoggingIn ? 'is-disabled' : ''}}"
            bindtap="handleLogin"
        >
            {{isLoggingIn ? '登录中...' : '微信一键登录'}}
        </view>

        <!-- 常用设置 -->
        <view class="settings-section">
            <text class="section-title">常用设置</text>
            <view class="settings-card">
                <view class="settings-item" wx:for="{{settings}}" wx:key="title">
                    <view class="item-icon {{item.icon}}"></view>
                    <view class="item-info">
                        <text class="item-title">{{item.title}}</text>
                        <text class="item-desc">{{item.desc}}</text>
                    </view>
                    <text wx:if="{{item.arrow}}" class="item-arrow">›</text>
                </view>
            </view>
        </view>

        <!-- 退出登录按钮（仅已登录时显示） -->
        <view
            wx:if="{{isLoggedIn}}"
            class="logout-button {{isLoggingOut ? 'is-disabled' : ''}}"
            bindtap="handleLogout"
        >
            退出登录
        </view>

        <view class="bottom-spacer"></view>
    </scroll-view>

    <!-- 修改昵称弹窗 -->
    <view wx:if="{{showEditModal}}" class="modal-mask" bindtap="closeEdit">
        <view class="modal-card" catchtap="">
            <text class="modal-title">修改昵称</text>
            <form bindsubmit="saveName">
                <input
                    class="name-input"
                    name="nickname"
                    type="nickname"
                    maxlength="12"
                    placeholder="请输入昵称"
                    value="{{draftName}}"
                    focus="{{showEditModal}}"
                />
                <view class="modal-actions">
                    <view class="modal-button ghost" bindtap="closeEdit">取消</view>
                    <button
                        class="modal-button primary {{isSaving ? 'is-disabled' : ''}}"
                        form-type="submit"
                    >
                        保存
                    </button>
                </view>
            </form>
        </view>
    </view>
</view>
